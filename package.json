{
  "name": "tdd-workshop",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@viz-js/viz": "^3.28.0",
    "express": "^5.2.1"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=10.7.0"
  },
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "lint": "eslint src public/js --ignore-pattern public/js/example.js",
    "smoke": "node scripts/smoke-static.mjs",
    "watch-test": "vitest",
    "test:ci": "vitest run",
    "test": "vitest run",
    "start": "node src/server.js"
  },
  "proxy": "http://localhost:4000",
  "devDependencies": {
    "eslint": "^10.8.1",
    "vitest": "^4.1.10"
  }
}
