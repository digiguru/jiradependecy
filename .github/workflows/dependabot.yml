version: 2
updates:
  - package-ecosystem: "npm" # Replace with your ecosystem (e.g., pip, bundler, github-actions)
    directory: "/"
    schedule:
      interval: "weekly" # Can be daily, weekly, or monthly

    # Group all dependencies together into a single PR
    groups:
      all-dependencies:
        patterns:
          - "*"
